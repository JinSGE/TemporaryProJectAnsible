# # 1. 연결 정보
# ansible_host: 172.16.6.63
# ansible_user: root

# # 2. API 서비스 공통 설정
# api_base_dir: "/opt/api"
# api_internal_port: 8000  # 컨테이너 내부 포트

# # 3. 배포할 API 리스트 (리스트 형식으로 관리하면 자동화가 더 편해집니다)
# deploy_apis:
#   - { name: "weather-api", host_port: 8001, image: "fastapi-weather:latest" }
#   - { name: "logistics-api", host_port: 8002, image: "fastapi-logistics:latest" }
#   - { name: "energy-api", host_port: 8003, image: "fastapi-energy:latest" }

# # 4. Docker 환경 설정
# docker_network_name: "api_network"